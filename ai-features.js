// ai-features.js: Contains all AI, accessibility, and performance JavaScript and integrates with OpenAI securely.
// OpenAI key uses environment variables or secure serverless endpoint, not hardcoded.

import { getWorkoutPlanAI, getArticleSummaryAI, handleAPIErrors } from './openai-service.js';

document.addEventListener('DOMContentLoaded', () => {
  // Blog post rendering, lazy image loading, modal accessibility, etc.
});
